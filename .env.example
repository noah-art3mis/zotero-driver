# Copy to .env and fill in. Create a key at zotero.org → Settings → Security → Applications.
# Your user ID is shown on the same page ("Your userID for use in API calls").
ZOTERO_API_KEY=
ZOTERO_USER_ID=
