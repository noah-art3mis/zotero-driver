# secrets — never commit, never read into agent context
.env
.env.*

# personal library data (canonical location is ~/.local/share/zelador/,
# but keep this net in case anything lands here)
data/

# python
__pycache__/
*.pyc
.venv/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
